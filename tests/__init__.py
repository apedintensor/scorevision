"""Sanity tests.

Run with: cd e:/sn44 && python -m pytest tests/

Critical test: `test_eval_parity.py` runs `eval/verify_against_official`
to confirm our local scorer still matches the turbovision implementation.
Re-run on every sync of turbovision or change to eval/.
"""
