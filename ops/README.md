# ops/

Cloud-GPU runbooks and per-machine notes.

Planned:
- `lium_setup.md` — provisioning a Lium box from scratch
- `lium_quickstart.sh` — one-shot env setup: git clone + uv sync + HF weights pull
- `runpod_setup.md` — alternative provider notes
- `runs.md` — chronological log of training runs (hyperparams, val score, $ cost)
