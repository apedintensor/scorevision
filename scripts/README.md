# scripts/

One-shot operational helpers. Not a Python package — run files directly.

Planned:
- `download_models.py` — pre-fetch all zoo model weights into HF cache
- `download_soccernet.py` — pull SoccerNet-v2 (gated, requires NDA + token)
- `fetch_score_samples.py` — pull Score-team sample videos referenced in Discord
- `refresh_intel.sh` — re-run discord export + leaderboard scrape
- `setup_wallet.sh` — Bittensor wallet creation walkthrough
